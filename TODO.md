# Blueprint work queue

- TODO(user): Choose the first supported Proxmox release after the cluster installation
  is tested and documented.
- TODO(user): Choose Debian/Ubuntu guest baselines and support windows.
- TODO(user): Choose Ansible and OpenTofu conventions after the private implementation
  proves which inputs are genuinely reusable.
- TODO(user): Decide whether public modules will be versioned independently or only by
  repository releases.
- TODO(user): Approve the public example naming and documentation-address conventions.
- TODO(user): Choose a secrets example that is safe, recoverable, and not mistaken for
  the exact private configuration.
