# Container application patterns

Compose examples should declare health checks, resource limits, restart behavior,
read-only filesystems where practical, persistent data, networks, secrets inputs,
logging, backup, upgrade, and rollback.
